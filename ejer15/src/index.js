const nombre = "Cobi";
const apellido = "Olimpic";

const persona = {
    nombre,
    apellido
}

//localStorage.setItem("persona", JSON.stringify({nombre, apellido}))
//localStorage.removeItem("persona", JSON.stringify({nombre, apellido}))

//sessionStorage.setItem("perona",JSON.stringify({nombre, apellido}))

//const fecha = new Date()
//document.cookie = `persona=${JSON.stringify(persona)};expires=${new Date(fecha.getTime() + 2 * 60000)}`


